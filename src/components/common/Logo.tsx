import { Link } from 'react-router';

const Logo = () => {
  return (
    <Link to="/">
      <span className="text-5xl font-sans text-primary font-black">
        Verdora
      </span>
    </Link>
  );
};

export default Logo;
